# ═══════════════════════════════════════════════════════════════
# Exoscale Deploy Kit — Credentials Template
# ═══════════════════════════════════════════════════════════════
#
# SETUP INSTRUCTIONS:
#   1. Copy this file:  cp .env.example .env
#   2. Fill in your credentials below
#   3. NEVER commit .env to git (it is in .gitignore)
#
# ═══════════════════════════════════════════════════════════════

# ── Exoscale API Credentials ──────────────────────────────────
# How to obtain:
#   1. Log in to https://portal.exoscale.com
#   2. Navigate to: IAM → API Keys → + Create
#   3. Name: "deploy-kit" (or any name)
#   4. Role: Unrestricted (or a scoped role with compute + network access)
#   5. Copy the Key and Secret shown ONCE (secret is not shown again)
EXO_API_KEY=EXOxxxxxxxxxxxxxxxxxxxx
EXO_API_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Docker Hub Credentials ────────────────────────────────────
# How to obtain:
#   1. Log in to https://hub.docker.com
#   2. Navigate to: Account Settings → Personal Access Tokens → Generate New Token
#   3. Name: "deploy-kit" (or any name)
#   4. Permissions: Read, Write, Delete
#   5. Copy the token shown ONCE (not shown again)
#
# NOTE: docker_hub_user (your username) goes in config.yaml, not here
DOCKER_HUB_TOKEN=dckr_pat_xxxxxxxxxxxxxxxxxxxxxx
