# Exoscale Deploy Kit — Python Dependencies
# Install: pip install -r requirements.txt
#
# Python 3.11+ required

# Exoscale Python SDK — REQUIRED (v0.16.1+ for correct zone-specific auth)
# CRITICAL LESSON: Manual HMAC signing fails — always use official SDK
exoscale>=0.16.1

# AWS S3-compatible client for Exoscale Object Storage (SOS) — optional
boto3>=1.28.0

# HTTP client (used by exoscale SDK)
requests>=2.31.0

# YAML parser for config.yaml
PyYAML>=6.0
