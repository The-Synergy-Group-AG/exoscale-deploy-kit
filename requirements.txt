# Exoscale Deploy Kit — Python Dependencies
# Install: pip install -r requirements.txt

# Exoscale Python SDK (v2 API — SKS, DBaaS, Security Groups, Compute)
exoscale>=0.16.1

# Object Storage (SOS — Exoscale S3-compatible API)
boto3>=1.34.0
botocore>=1.34.0

# YAML config loading
PyYAML>=6.0

# Environment variable loading (.env file)
python-dotenv>=1.0.0
