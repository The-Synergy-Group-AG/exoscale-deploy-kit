# Exoscale Deploy Kit — Python Dependencies
# Install: pip install -r requirements.txt
#
# Version floors set to latest known-safe releases (2025-02).
# Dependabot / pip-audit: no known vulnerabilities as of this commit.

# ── Core: Exoscale Python SDK (v2 API — SKS, DBaaS, Security Groups, Compute) ──
exoscale>=0.16.1

# ── Core: Exoscale Object Storage (SOS — S3-compatible) ──
boto3>=1.42.54
botocore>=1.42.54

# ── Config & Env ──
PyYAML>=6.0.3
python-dotenv>=1.2.1

# ── Security: explicit safe floors for high-risk transitive deps ──
# urllib3  CVE-2024-37891 (HIGH)    fixed >=2.2.2  — pulled in by botocore/requests
# requests CVE-2024-35195 (MOD)     fixed >=2.32.2 — pulled in by exoscale SDK
# certifi  GHSA-xqr8-7jhe-pchp (H) fixed >=2024.7.4 — pulled in by requests
urllib3>=2.2.2
requests>=2.32.2
certifi>=2024.7.4
